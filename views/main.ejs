<%- include ('partials/header') %> 
      <div class="Main title-heading border">
        <img src="images/MrGhostPFP.png" alt="" class="MyPfp" />
      </div>
        <div id="info">
          <% posts.forEach(function(post) { %>
            <h2><%= post.title %> </h2>
            <p><%= post.paragraph.substring(0, 100) + "..."%> <a href="/posts/<%= post._id %>">Read More</a></p>
          <% }) %>
        </div>
      
<%- include ('partials/footer') %>
